# ==============================================
# PicShare 部署环境变量配置示例
# ==============================================
# 复制此文件为 .env.deploy，然后填入你的实际配置
# 使用方式: source .env.deploy && bash deploy-remote.sh
# ==============================================

# ========== ECS 服务器配置 ==========
# ECS 公网 IP 或域名
ECS_HOST=47.xxx.xxx.xxx
# SSH 用户名（默认 root）
ECS_USER=root
# SSH 端口（默认 22）
ECS_PORT=22
# SSH 密钥路径（可选，使用密码认证时可省略）
# ECS_KEY=/path/to/your/ssh/key.pem

# ========== 数据库配置 (RDS) ==========
# RDS 内网地址（推荐）或公网地址
DB_HOST=rm-xxxxx.mysql.rds.aliyuncs.com
# 数据库端口
DB_PORT=3306
# 数据库名称
DB_NAME=picshare
# 数据库用户名
DB_USER=picshare
# 数据库密码
DB_PASSWORD=your_secure_password_here

# ========== OSS 配置 ==========
# 阿里云 AccessKeyId
ALIYUN_AK=your_access_key_id
# 阿里云 AccessKeySecret
ALIYUN_SK=your_access_key_secret
# OSS Bucket 名称
OSS_BUCKET=your-bucket-name
# OSS 区域（如：oss-cn-hangzhou, oss-cn-beijing）
OSS_REGION=oss-cn-hangzhou

# ========== 域名配置（可选）==========
# 你的域名（如无域名可使用 ECS IP）
DOMAIN=www.yourdomain.com

# ========== 管理员配置（可选）==========
# 管理员邮箱
ADMIN_EMAIL=admin@yourdomain.com
# 管理员密码（请使用强密码）
ADMIN_PASSWORD=YourSecurePassword123!

# ========== 邮件配置（可选）==========
# 阿里云邮件推送 SMTP 配置
# SMTP_HOST=smtpdm.aliyun.com
# SMTP_PORT=465
# SMTP_USER=noreply@yourdomain.com
# SMTP_PASS=your_smtp_password
# SMTP_FROM=PicShare <noreply@yourdomain.com>
